---
const navLinks = [
    { href: '#features', label: 'Features' },
    { href: '#pricing', label: 'Pricing' },
    { href: 'https://github.com/vdavid/prvw', label: 'GitHub', external: true },
]
---

<header
    class="fixed top-0 left-0 right-0 z-50 border-b border-[var(--color-border-subtle)] bg-[var(--color-background)]/80 backdrop-blur-xl"
>
    <nav class="mx-auto flex h-16 max-w-5xl items-center justify-between px-6">
        <!-- Logo -->
        <a href="/" class="flex items-center gap-2 text-xl font-semibold transition-opacity hover:opacity-80" aria-label="Prvw home">
            <img src="/icon-40.png" alt="" width="28" height="28" class="rounded-md" />
            <span class="text-[var(--color-accent)]">Prvw</span>
        </a>

        <!-- Desktop navigation -->
        <div class="hidden items-center gap-8 md:flex">
            {
                navLinks.map((link) => (
                    <a
                        href={link.href}
                        target={link.external ? '_blank' : undefined}
                        rel={link.external ? 'noopener noreferrer' : undefined}
                        class="text-sm text-[var(--color-text-secondary)] transition-colors duration-200 hover:text-[var(--color-text-primary)]"
                    >
                        {link.label}
                        {link.external && (
                            <span class="ml-1 text-xs" aria-hidden="true">
                                ↗
                            </span>
                        )}
                    </a>
                ))
            }

            <!-- Download CTA -->
            <a
                href="https://github.com/vdavid/prvw/releases"
                class="rounded-lg bg-[var(--color-accent)] px-4 py-2 text-sm font-semibold text-[var(--color-accent-contrast)] transition-colors duration-200 hover:bg-[var(--color-accent-hover)]"
            >
                Download
            </a>
        </div>

        <!-- Mobile: hamburger -->
        <div class="flex items-center md:hidden">
            <button
                type="button"
                class="mobile-menu-toggle flex items-center justify-center"
                aria-label="Open menu"
                aria-expanded="false"
                aria-controls="mobile-menu"
                data-mobile-menu-toggle
            >
                <svg
                    width="24"
                    height="24"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    aria-hidden="true"
                >
                    <line class="hamburger-line hamburger-line--top" x1="3" y1="6" x2="21" y2="6"></line>
                    <line class="hamburger-line hamburger-line--middle" x1="3" y1="12" x2="21" y2="12"></line>
                    <line class="hamburger-line hamburger-line--bottom" x1="3" y1="18" x2="21" y2="18"></line>
                </svg>
            </button>
        </div>
    </nav>

    <!-- Mobile menu -->
    <div id="mobile-menu" class="mobile-menu md:hidden" inert role="navigation" aria-label="Mobile navigation">
        <div class="mobile-menu__content">
            {
                navLinks.map((link) => (
                    <a
                        href={link.href}
                        target={link.external ? '_blank' : undefined}
                        rel={link.external ? 'noopener noreferrer' : undefined}
                        class="mobile-menu__link"
                        data-mobile-nav-link
                    >
                        {link.label}
                        {link.external && (
                            <span class="ml-1 text-xs" aria-hidden="true">
                                ↗
                            </span>
                        )}
                    </a>
                ))
            }

            <div class="mt-2" data-mobile-nav-link>
                <a
                    href="https://github.com/vdavid/prvw/releases"
                    class="inline-flex w-full items-center justify-center rounded-lg bg-[var(--color-accent)] px-4 py-3 text-sm font-semibold text-[var(--color-accent-contrast)] transition-colors hover:bg-[var(--color-accent-hover)]"
                >
                    Download
                </a>
            </div>
        </div>
    </div>
</header>

<style>
    .mobile-menu-toggle {
        width: 44px;
        height: 44px;
        padding: 10px;
        color: var(--color-text-primary);
        background: none;
        border: none;
        cursor: pointer;
        border-radius: 0.5rem;
        transition: background-color var(--duration-normal);
    }

    .mobile-menu-toggle:hover {
        background-color: var(--color-surface-elevated);
    }

    .mobile-menu-toggle:focus-visible {
        outline: 2px solid var(--color-accent);
        outline-offset: 2px;
    }

    .hamburger-line {
        transition:
            transform var(--duration-normal) var(--ease-out-expo),
            opacity var(--duration-normal) var(--ease-out-expo);
        transform-box: fill-box;
        transform-origin: center;
    }

    .mobile-menu-toggle[aria-expanded='true'] .hamburger-line--top {
        transform: translateY(6px) rotate(45deg);
    }

    .mobile-menu-toggle[aria-expanded='true'] .hamburger-line--middle {
        opacity: 0;
    }

    .mobile-menu-toggle[aria-expanded='true'] .hamburger-line--bottom {
        transform: translateY(-6px) rotate(-45deg);
    }

    .mobile-menu {
        max-height: 0;
        overflow: hidden;
        border-top: 1px solid transparent;
        visibility: hidden;
        transition:
            max-height var(--duration-slow) var(--ease-out-expo),
            visibility 0s var(--duration-slow);
    }

    .mobile-menu:not([inert]) {
        max-height: 400px;
        border-top-color: var(--color-border-subtle);
        visibility: visible;
        transition:
            max-height var(--duration-slow) var(--ease-out-expo),
            visibility 0s;
    }

    .mobile-menu__content {
        display: flex;
        flex-direction: column;
        padding: 8px 24px 24px;
        gap: 4px;
    }

    .mobile-menu__link {
        display: flex;
        align-items: center;
        padding: 12px 16px;
        font-size: 0.9375rem;
        color: var(--color-text-secondary);
        text-decoration: none;
        border-radius: 0.5rem;
        min-height: 44px;
        transition:
            background-color var(--duration-normal),
            color var(--duration-normal);
    }

    .mobile-menu__link:hover {
        background-color: var(--color-surface-elevated);
        color: var(--color-text-primary);
    }

    .mobile-menu__link:focus-visible {
        outline: 2px solid var(--color-accent);
        outline-offset: -2px;
    }

    @media (prefers-reduced-motion: reduce) {
        .mobile-menu,
        .hamburger-line {
            transition: none !important;
        }
    }
</style>

<script>
    document.addEventListener('astro:page-load', () => {
        const menuToggle = document.querySelector<HTMLButtonElement>('[data-mobile-menu-toggle]')
        const mobileMenu = document.getElementById('mobile-menu')

        if (!menuToggle || !mobileMenu) return
        if (menuToggle.dataset.bound) return
        menuToggle.dataset.bound = 'true'

        const openMobileMenu = () => {
            mobileMenu.removeAttribute('inert')
            menuToggle.setAttribute('aria-expanded', 'true')
            menuToggle.setAttribute('aria-label', 'Close menu')
            const firstLink = mobileMenu.querySelector<HTMLElement>('[data-mobile-nav-link]')
            setTimeout(() => firstLink?.focus(), 50)
        }

        const closeMobileMenu = () => {
            mobileMenu.setAttribute('inert', '')
            menuToggle.setAttribute('aria-expanded', 'false')
            menuToggle.setAttribute('aria-label', 'Open menu')
        }

        menuToggle.addEventListener('click', () => {
            const isOpen = !mobileMenu.hasAttribute('inert')
            if (isOpen) closeMobileMenu()
            else openMobileMenu()
        })

        mobileMenu.querySelectorAll('[data-mobile-nav-link]').forEach((link) => {
            link.addEventListener('click', () => closeMobileMenu())
        })

        document.addEventListener('keydown', (e: KeyboardEvent) => {
            if (e.key === 'Escape' && !mobileMenu.hasAttribute('inert')) {
                closeMobileMenu()
                menuToggle.focus()
            }
        })

        document.addEventListener('click', (e: MouseEvent) => {
            if (!mobileMenu.hasAttribute('inert')) {
                const header = menuToggle.closest('header')
                if (header && !header.contains(e.target as Node)) {
                    closeMobileMenu()
                }
            }
        })
    })
</script>
