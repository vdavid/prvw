{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window and dialog windows",
  "windows": ["main", "about", "settings"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "store:default",
    "log:default"
  ]
}
