//! Settings-window panels that live inside the Settings feature itself.
//!
//! Panels tied to individual features (color, zoom, file associations) live under
//! their feature module as `settings_panel`.

pub(super) mod general;
pub(super) mod raw;
